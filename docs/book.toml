[book]
authors = ["Mitchell Robert Vollger"]
language = "en"
multilingual = false
src = "src"
title = "fibertools"

[build]
build-dir = "book"                # the directory where the output is placed
create-missing = false             # whether or not to create missing pages
use-default-preprocessors = true  # use the default preprocessors
extra-watch-dirs = []             # directories to watch for triggering builds


[output.html]
default-theme = "dark"
preferred-dark-theme = "navy"
git-repository-url = "https://github.com/fiberseq/fibertools-rs"
git-repository-icon = "fa-github"
edit-url-template = "https://github.com/fiberseq/fibertools-rs/edit/main/docs/{path}"

[output.html.fold]
enable = true    # whether or not to enable section folding
level = 1        # the depth to start folding

[preprocessor.toc]
command = "mdbook-toc"
renderer = ["html"]
max-level = 4


#[preprocessor.auto-links]
#command = "python auto-links.py"

